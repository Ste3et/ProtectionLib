image: maven:3-jdk-8

stages:
- build
- tag
- deploy

ProtectionLib:
  stage: build
  script:
     - mvn clean package
  artifacts:
     paths:
     - target/*.jar
  only:
    - master
    
ProtectionLib-repo:
   stage: deploy
   script:
     - 'mvn deploy -s ci_settings.xml'
   only:
     - master